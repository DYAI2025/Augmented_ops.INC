{
  "name": "augmented-ops",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "rm -rf dist",
    "lint": "node --check server.js && node --check scripts/build.mjs",
    "start": "node server.js",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
